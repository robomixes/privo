[versions]
agp = "8.9.3"
kotlin = "2.3.20"
coreKtx = "1.16.0"
lifecycleRuntimeKtx = "2.9.0"
activityCompose = "1.10.1"
composeBom = "2025.03.01"
camerax = "1.4.2"
onnxruntime = "1.22.0"
navigation = "2.9.0"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycleRuntimeKtx" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycleRuntimeKtx" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }
androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation" }

# CameraX
androidx-camera-core = { group = "androidx.camera", name = "camera-core", version.ref = "camerax" }
androidx-camera-camera2 = { group = "androidx.camera", name = "camera-camera2", version.ref = "camerax" }
androidx-camera-lifecycle = { group = "androidx.camera", name = "camera-lifecycle", version.ref = "camerax" }
androidx-camera-view = { group = "androidx.camera", name = "camera-view", version.ref = "camerax" }
androidx-camera-video = { group = "androidx.camera", name = "camera-video", version.ref = "camerax" }

# ONNX Runtime
onnxruntime-android = { group = "com.microsoft.onnxruntime", name = "onnxruntime-android", version.ref = "onnxruntime" }

# ExifInterface
androidx-exifinterface = { group = "androidx.exifinterface", name = "exifinterface", version = "1.3.7" }

# AppCompat (needed for per-app language switching)
androidx-appcompat = { group = "androidx.appcompat", name = "appcompat", version = "1.7.0" }

# Biometric
androidx-biometric = { group = "androidx.biometric", name = "biometric", version = "1.1.0" }

# WorkManager (periodic missed-sweep for reminders)
androidx-work-runtime = { group = "androidx.work", name = "work-runtime-ktx", version = "2.9.1" }

# Coroutines Play Services (for .await() on ML Kit Tasks)
kotlinx-coroutines-play-services = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-play-services", version = "1.9.0" }

# ML Kit
mlkit-translate = { group = "com.google.mlkit", name = "translate", version = "17.0.3" }

# Tesseract 5 (OCR — replaces ML Kit text-recognition, Track A1.3). The
# tesseract4android library is the maintained Android wrapper around the
# upstream Tesseract C++ engine. The `-openmp` variant uses OpenMP for
# multi-threaded recognition (~2x faster on multi-core devices). Hosted
# on JitPack only — Maven Central doesn't carry this artifact, so the
# JitPack repo is added in settings.gradle.kts. The group uses `.` not
# `:` between user/repo because we're pulling a subproject of the
# multi-module Tesseract4Android Gradle build.
tesseract4android = { group = "com.github.adaptech-cz.Tesseract4Android", name = "tesseract4android-openmp", version = "4.9.0" }

# ZXing (QR code generation)
zxing-core = { group = "com.google.zxing", name = "core", version = "3.5.3" }

# LiteRT-LM (Gemma 4 on-device LLM inference)
# Pinned to a specific version (was "+") for reproducible builds — required for
# F-Droid and stops upstream regressions from silently breaking the next clean
# build. Bumping this is a deliberate decision; test vision after every bump.
litertlm-android = { group = "com.google.ai.edge.litertlm", name = "litertlm-android", version = "0.11.0" }

# SQLCipher (encrypted SQLite)
sqlcipher = { group = "net.zetetic", name = "sqlcipher-android", version = "4.6.1" }
sqlite-ktx = { group = "androidx.sqlite", name = "sqlite-ktx", version = "2.4.0" }

# Wi-Fi Transfer
nanohttpd = { group = "org.nanohttpd", name = "nanohttpd", version = "2.3.1" }

# PdfBox-Android — text-layer extraction from already-imported PDFs (Phase 2
# of "Ask My Documents"). MIT licensed, AGPL-compatible. ~3 MB at runtime.
pdfbox-android = { group = "com.tom-roush", name = "pdfbox-android", version = "2.0.27.0" }

# Testing
junit = { group = "junit", name = "junit", version = "4.13.2" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
